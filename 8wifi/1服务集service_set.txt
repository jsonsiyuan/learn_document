服务集（service set）是无线局域网中的一个术语。
用来描述802.11 无线网络的构成单元。
使用服务集标识符（ssid）作为识别。

	IBSS: 独立基本服务集：只要是一些点对点，不要过中间（路由器）的网络通信
	BSS ：基本服务集 正常的一些网络类型
	ESS:  扩展服务集 扩展网络 网桥，中继器等
	
ID 分类：
	bssid：类似于MAC地址（在无线网络中无该概念。）一般取MA	C地址作为bssid。
	essid：一个抽象的名称，基本使用ssid表示。